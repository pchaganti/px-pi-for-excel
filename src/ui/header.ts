/**
 * Pi for Excel — Header bar component.
 *
 * Minimal branding bar. Model picker and status live in the footer status bar.
 */

/** CSS for the header. Injected into the document once. */
export const headerStyles = `
  /* Header removed — model picker + status moved to footer status bar.
     Keeping this export so existing imports don't break. */
`;
