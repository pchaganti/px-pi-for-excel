title: "Current state: 14 tools, all always-on"
subtitle: "Every tool definition sent on every turn (~2–3k tokens)"
tracker: "Current State"
slide_layout: "2/3"

sidebar:
  - { text: "4 tools are rarely used", bold: true, color: accent1, size: 16 }
  - "get_range_as_csv — same as read_range with different output"
  - ""
  - "read_selection — auto-context already reads the selection"
  - ""
  - "get_all_objects — overview already reports object counts"
  - ""
  - "get_recent_changes — auto-context already injects changes"

table:
  rows: 8
  cols: 3
  has_col_header: true
  has_row_header: true
  column_widths: [1, 3, 1]

  fonts:
    body: 12
    header: 13

  col_headers:
    - "Tools (14 total)"
    - "Use"

  row_headers:
    - "Read (5)"
    - "Write (2)"
    - "Navigate (1)"
    - "Structure (1)"
    - "Format (2)"
    - "Inspect (1)"
    - "View + Context (2)"

  cells:
    - ["overview, read_range, csv, selection, objects", "Core + 3 rare"]
    - ["write_cells, fill_formula", "Core"]
    - ["search_workbook", "Common"]
    - ["modify_structure", "Common"]
    - ["format_cells, conditional_format", "Common"]
    - ["trace_dependencies", "Occasional"]
    - ["view_settings, get_recent_changes", "Occasional"]
