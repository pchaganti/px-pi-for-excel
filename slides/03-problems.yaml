title: "Three problems"
subtitle: "Overlap, ambiguity, and an unsustainable trajectory"
tracker: "Problems"
slide_layout: "2/3"

sidebar:
  - { text: "The trajectory", bold: true, color: accent1, size: 16 }
  - "Issue #18 proposes charts, tables, validation, sorting, named ranges, comments…"
  - ""
  - "That's **14 → 22+ tools**."
  - ""
  - "At 20+ tools the model swims through schemas every turn."

table:
  rows: 5
  cols: 3
  has_col_header: true
  has_row_header: true
  column_widths: [2, 3, 3]

  fonts:
    body: 12
    header: 13

  col_headers:
    - "Overlap"
    - "Why it's confusing"

  row_headers:
    - "read_range vs\nget_range_as_csv"
    - "read_selection vs\nauto-context"
    - "get_all_objects vs\noverview"
    - "get_recent_changes\nvs auto-context"

  cells:
    - ["Same operation, different output format", "Model must choose — usually picks wrong one"]
    - ["Identical result to auto-injected context", "Model calls tool that's already run"]
    - ["Overview already reports object counts", "Useful only when we can manipulate objects"]
    - ["Auto-context injects this every turn", "Model never needs to call it explicitly"]
