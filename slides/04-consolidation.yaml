title: "Proposed consolidation: 14 → 10"
subtitle: "Remove 4 tools by absorption, absorb 2 proposed without adding them"
tracker: "Proposal"
slide_layout: "Default"

table:
  rows: 7
  cols: 3
  has_col_header: true
  has_row_header: true
  column_widths: [3, 2, 4]
  placeholders: false

  col_headers:
    - "Absorb into"
    - "How"

  row_headers:
    - "get_range_as_csv"
    - "read_selection"
    - "get_all_objects"
    - "get_recent_changes"
    - "find_by_label (#7)"
    - "get_sheet_summary (#8)"

  cells:
    - ["read_range", "Add format: table | csv | detailed"]
    - ["Auto-context", "Already injected every turn"]
    - ["get_workbook_overview", "Add optional sheet param for sheet-level detail"]
    - ["Auto-context", "Already injected every turn"]
    - ["search_workbook", "Add context_rows param for surrounding rows"]
    - ["get_workbook_overview", "Add optional sheet param (same as get_all_objects)"]
